# PosterWords 官网部署说明

## 文件结构

```
website/
├── index.html          # 英文首页（SEO 主页面）
├── style.css           # 响应式样式
├── robots.txt          # 搜索引擎爬虫指引
├── sitemap.xml         # 站点地图
├── zh/
│   └── index.html      # 中文版页面
├── images/
│   ├── logo.svg        # Logo（已有占位）
│   ├── hero-mockup.png # 📌 需要：App 截图 mockup（建议 720×1440）
│   ├── og-cover.png    # 📌 需要：OG 分享图（1200×630）
│   ├── favicon-32.png  # 📌 需要：32px favicon
│   ├── favicon-16.png  # 📌 需要：16px favicon
│   └── apple-touch-icon.png # 📌 需要：180px touch icon
└── 部署说明.md
```

## 上线前待替换

| 占位符 | 替换为 |
|--------|--------|
| `YOUR_APP_ID` | App Store 中的实际 Apple ID（数字） |
| `https://posterwords.app/` | 实际域名（如果不同） |
| `images/hero-mockup.png` | App 真实截图或 3D mockup |
| `images/og-cover.png` | 社交分享封面图 |
| `support@posterwords.app` | 实际支持邮箱 |

## 推荐部署方式

### 方案 A：Cloudflare Pages（推荐）
1. 将 `website/` 目录推送到 Git 仓库
2. Cloudflare Pages → 连接 Git → 构建设置选 "None"（纯静态）
3. 自定义域名 `posterwords.app`
4. 自动启用全球 CDN、HTTPS、HTTP/2

### 方案 B：GitHub Pages
1. 创建 repo `posterwords/posterwords.github.io`
2. 将文件推送到 main 分支
3. Settings → Pages → Source: main branch
4. 自定义域名在 DNS 添加 CNAME

### 方案 C：Vercel
1. Import repo → Framework: "Other"
2. Output directory: `website`
3. 自定义域名绑定

## SEO 优化清单

- [x] 语义化 HTML5 结构
- [x] Schema.org SoftwareApplication 结构化数据
- [x] Schema.org FAQPage 结构化数据
- [x] Open Graph & Twitter Card 标签
- [x] Apple Smart App Banner
- [x] hreflang 多语言标签（英文 + 中文）
- [x] Canonical URL
- [x] robots.txt + sitemap.xml
- [x] 响应式设计（移动优先）
- [x] 语义化标题层级（H1 → H2 → H3）
- [x] Alt 文本
- [x] 无障碍 ARIA 标签
- [ ] 📌 上线后提交 sitemap 到 Google Search Console
- [ ] 📌 上线后提交到 Bing Webmaster Tools
- [ ] 📌 注册 Google Analytics 或 Plausible 分析

## 搜索引擎提交

上线后需手动提交：
1. **Google Search Console**: https://search.google.com/search-console
   - 验证域名所有权 → 提交 sitemap.xml
2. **Bing Webmaster Tools**: https://www.bing.com/webmasters
   - 同样验证 + 提交 sitemap
3. **Apple**: App Store 的 Support URL 和 Privacy URL 改为官网地址

## 持续优化建议

1. **博客/内容营销**：添加 `/blog/` 目录，写中文学习相关文章（长尾关键词）
2. **App Store 链接互通**：App Store 描述中加入官网链接
3. **结构化数据更新**：上架后补充真实评分数据到 aggregateRating
4. **页面速度**：图片使用 WebP 格式，开启 gzip/brotli 压缩
